//math.ceil -rounds  a number to the nearest intergrer
//math.floor- rounds a number down to the nearest interger
//Math.round - rounds to the closest integer (either up or down)
let num = 12.345;
console.log(Math.ceil(num)); //13
console.log(Math.floor(num)); //12
console.log(Math.round(num)); //12</s>
//math.max-returns the highest number from the list of arguements 
//Math.min-returns the lowest Number.
//generating random numbers 
//math.random(); generates random numbers which can be used together with math.floor